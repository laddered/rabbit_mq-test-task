<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}RabbitMQ Demo{% endblock %}</title>
    {{ encore_entry_link_tags('app') }}
</head>
<body>
    <div class="w-100 py-3 px-4 border-bottom bg-light">
        <div class="d-flex justify-content-between align-items-center">
            <div class="text-start">
                <span class="fw-bold">Добро пожаловать!</span> <span class="text-muted">Пожалуйста, войдите или зарегистрируйтесь, чтобы продолжить.</span>
            </div>
            <div class="d-flex justify-content-end align-items-center">
                <a href="/login" class="btn btn-outline-primary me-2">Вход</a>
                <a href="/register" class="btn btn-primary">Регистрация</a>
            </div>
        </div>
    </div>
    <div class="container mt-4">
        {% block body %}{% endblock %}
    </div>
    <footer class="footer mt-auto py-3">
        <div class="container text-center text-muted">
            © {{ "now"|date("Y") }} RabbitMQ Demo
        </div>
    </footer>
    {{ encore_entry_script_tags('app') }}
</body>
</html> 